# Handoff — I-017: Shell redesign slice 4 — typography hierarchy and register visual identity

**Branch:** `feat/I-017-typography-hierarchy`

**Dependencies:** I-016 merged

**Context:**
Slice 4 of the shell redesign (`docs/shell-redesign-plan.md`). Fonts are loaded (Slice 1). Layout and navigation are settled (Slices 2–3). Now apply a proper type scale across all surfaces and make the register feel like a precision ledger rather than a generic HTML table. This is purely CSS and minor component-level class additions — no behavioral changes.

**Acceptance criteria:**

1. **Type scale variables** added to `:root` in `styles.css`:
   ```css
   --text-2xs: 10px;
   --text-xs:  11px;
   --text-sm:  12px;
   --text-base: 13px;
   --text-md:  14px;
   --text-lg:  16px;
   --text-xl:  20px;
   --text-2xl: 24px;

   --weight-normal:   400;
   --weight-medium:   500;
   --weight-semibold: 600;
   --weight-bold:     700;
   ```

2. **Register rows** — updated in `LedgerRegisterPanel.tsx` and `styles.css`:
   - Date cell: `font-family: var(--mono-font)`, `font-size: var(--text-sm)`, `color: var(--text-muted)`, fixed width column (`--date-col-width: 88px`)
   - Description cell: `font-size: var(--text-sm)`, `color: var(--text)`, flex-grow
   - Payee cell: `font-size: var(--text-sm)`, `color: var(--text-muted)`
   - Amount cell: `font-family: var(--mono-font)`, `font-size: var(--text-sm)`, `font-variant-numeric: tabular-nums`, right-aligned, fixed width (`--amount-col-width: 100px`), `--amount-positive` / `--amount-negative` / `--text-muted` for zero
   - Running balance cell: `font-family: var(--mono-font)`, `font-size: var(--text-sm)`, `font-variant-numeric: tabular-nums`, right-aligned, fixed width (`--balance-col-width: 110px`), `color: var(--text-muted)`
   - Row separator: `border-bottom: 1px solid color-mix(in srgb, var(--border) 35%, transparent)` — very subtle
   - Comfortable row height: `--row-height: 40px` (reduced from 48px)
   - Compact row height: `--row-height: 28px` (unchanged)
   - Horizontal padding: `12px` left and right

3. **COA sidebar** — updated in `CoaSidebar.tsx` and `styles.css`:
   - Account type section headers ("asset", "liability", etc.): `font-size: var(--text-2xs)`, `font-weight: var(--weight-semibold)`, `color: var(--text-muted)`, `text-transform: uppercase`, `letter-spacing: 0.08em`
   - Account name: `font-size: var(--text-sm)`, `font-weight: var(--weight-normal)`, `color: var(--text)`
   - Account code: `font-family: var(--mono-font)`, `font-size: var(--text-xs)`, `color: var(--text-muted)`
   - Account balance: `font-family: var(--mono-font)`, `font-size: var(--text-sm)`, right-aligned, positive/negative color

4. **Register tab / active account header** — the tab label showing the active account name:
   - Account name: `font-size: var(--text-xl)`, `font-weight: var(--weight-semibold)`, Geist (inherits from `--display-font`)
   - Account type: `font-size: var(--text-xs)`, `color: var(--text-muted)`, displayed as a chip or inline label

5. **Overview cards** (dashboard):
   - Metric number: `font-size: var(--text-2xl)`, `font-weight: var(--weight-bold)`
   - Card label: `font-size: var(--text-sm)`, `font-weight: var(--weight-medium)`, `color: var(--text-muted)`
   - Card summary: `font-size: var(--text-xs)`, `color: var(--text-muted)`

6. **Status bar**: `font-size: var(--text-xs)` (already uses `--mono-font`; adjust size only if needed)

7. Both density modes (`comfortable` / `compact`) render correctly at the new row heights. No layout overflow or clipping.

8. Amounts in all register rows align vertically in their column — the fixed-width columns ensure this without CSS grid tricks.

9. `pnpm --filter @tally/web typecheck` passes.
10. `pnpm ci:verify` passes.

**Key files:**

- `apps/web/src/app/styles.css` — type scale variables; register row styles; COA sidebar styles; overview card styles; row height updates
- `apps/web/src/app/LedgerRegisterPanel.tsx` — add className hooks for date/amount/balance cells if not already present to target with the new CSS
- `apps/web/src/app/CoaSidebar.tsx` — add className hooks for section headers, account code, account balance if not already present

**Do not touch:**
- Any files outside `apps/web/`
- Any behavioral logic, state, or API calls
- Font loading (already done in Slice 1)
- Inspector panel styles (done in Slice 3)

**Risk:** R2

**Rollback:** Revert CSS and minor className changes in `apps/web/` — no other packages affected

**Final step:** push the branch and open a PR using `.github/PULL_REQUEST_TEMPLATE.md`. Fill out all sections. `pnpm ci:verify` must pass before opening the PR. Append a one-line completion entry to `docs/project-status.md` before opening the PR. Risk is R2 — leave the PR open for maintainer review, do not merge.
