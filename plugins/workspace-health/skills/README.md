# Workspace Health

Use this plugin for repository verification and CI-adjacent checks:

- `pnpm typecheck`
- `pnpm test`
- `pnpm coverage`
- `pnpm ci:verify`
- targeted verification for a single app or package

Summaries should emphasize the smallest failing gate, the likely root cause, and the next corrective step.
